# Контекст проекта PegasLogistic v1

Живой контекст: что это за система, как устроена и что происходит сейчас.
Обновляется минимум каждым пятым коммитом (правило в CLAUDE.md).
Секретов здесь нет и быть не должно: токены/пароли живут в настройках
приложения и на сервере.

Обновлено: 2026-09-22 (ревизия на 520-м коммите).
Изменения ревизии: ЕДИНЫЙ КАНОН ОТЧЁТНОСТИ (разбор руководителя
«в рейсе 117 при КТГ 91% не бьётся») — все четыре поверхности
(«📆 Отчёт дня», утренняя сводка 07:00, «📊 Эксплуатация» /ops-report,
«📈 Показатели периода») считают из одной пары функций
parkReportData + opsReportData: «работали за день» = уникальные машины,
«на линии среднесуточно» = живой ряд (идущие рейсы + перегоны), простой
по причинам из диспозиций, «без причины» = парк − линия − объяснённое,
КТГ = (парк − ремонт − выведенные)/парк, КВЛ = линия/техисправные,
КИП — по закрытым рейсам; /api/reports отдаёт блок canon, фронт
руководителя переопределяет им плитки/каскад/водопад/машино-дни;
будущие дни периода в машино-дни не входят (opsReportData обрезает
период по «завтра»). Урок: сводку собирать из ОДНОЙ канон-функции,
методика «середина дня» давала КИП=100% всегда.
Предыдущая ревизия (515-й, догоняющая на 517-м):
Изменения ревизии: главная руководителя = «📊 Эксплуатация» (строка
«прямо сейчас» /api/ops-now + iframe /ops-report; прежний экран — вид
«Показатели периода»); отчёт эксплуатации /ops-report за период
(количественные показатели в машинах, простой по причинам, опоздания
П/В по trip_stops, живые графики; канон вынесен в parkReportData;
ежедневная сводка 07:15 в ТГ и ленту руководителям); ИНЦИДЕНТ
«Погрузок: 0» — тест-сервер с копией базы слал в живой ТГ: tgApi
глушится вне NODE_ENV=production; график: ввод по-Excel (выделение →
код) с фиксами гонки sync и ключей выделения, назначение с поиском,
компактная панель (месяц в обзоре), слиты экипажи-дубли E019/E025;
перецепка с датой (задним числом/плановая, сторож применяет в 00:00
МСК с уведомлением; 🔗 в графике, /api/trailer-timeline); гейт этапа 3:
покрытие плана 85% (сторож уведомит), пока ~65%.
Предыдущая ревизия (505-й):
Изменения ревизии: ПЕРЕСТРОЙКА РЕСУРСА (план утверждён 17.09) — этап 1:
новый «График работы» /schedule (прототип руководителя за авторизацией,
src/pages/, хранение экипажей JSON-блоками schedule_crews + журнал
schedule_log, протокол POST /api/schedule/sync, достройка из планера
/api/schedule/augment, кнопка «📋 График (бета)» в Ресурсе); этап 2:
мосты без циклов — П/РП плана → пересменки-диспозиции (syncShiftBridge,
пометка «из графика»), рейсы/перегоны → факт-слой (runScheduleAutoFact
ежечасно, счётчик покрытия); закрепление за логистами убрано из
концепции (поле пусто, зарезервировано под бригады со старшими
бригадирами — проект у руководителя); обкатка чт-пт, этап 3
(явка/табель ↔ факт, доплаты сверхвахты, потребители) — суббота 19.09;
этап 4 — вывод старой таблицы закреплений.
Предыдущая ревизия (500-й):
Изменения ревизии: Ресурс — правило «фамилия только на период
закрепления» (лента в ганте + график, sk-gap после пересменки без
назначенного), закрепление протяжкой по графику с окошком, карточка
«📌 Водители сцепки», замена по факту с подрезкой периодов
(createDriverAssignment рвёт/укорачивает пересечения на машине,
транзакция + аудит); круги — постоянные кольца К-1/К-2п/К-4а/К-5
(routes.ring_key, удаление 409, закрытие с причиной → руководителю),
src/rings.mjs (живой зазор стыковки 0,2–1,5/фолбэк 0,85; циклы
последовательным матчингом за 28 дн), панель «⭕ Загрузка кругов»,
полоса «⭕ Круги» продажам + разовое задание (К3/Планета Колбас/К4а);
фиксы: renderInto глушил дорисовку графика (изменения без F5 не были
видны), дубли обработчиков кисти на вечном контейнере.
Предыдущая ревизия (490-й): Ремзона этап 1 (repair_orders/repair_jobs,
роль mechanic, Тарасов); прицепы trailers + автотип от прицепа, П41 =
11/11; звенья trip_segments — экономика перецепа по честным км; пробег
прицепа для ТОиР (/api/trailer-mileage); прогноз месяца по медианам
дня недели; «💼 Уже в кармане».
## Что это

TMS логистической компании «ПегасЛогистик»: планирование и контроль
рейсов рефрижераторного парка (~128 сцепок), конвейер заявок
продажи → логист → диспетчер → 1С, контроль на линии по данным
GPS/CAN (Пилот), экономика рейсов и самонастраивающиеся нормативы.
Прод живой, на нём работают сотрудники. Язык всего — русский.

## Архитектура

- Node.js ≥22, ES-модули, без npm-зависимостей; БД — node:sqlite
  (DatabaseSync), файл data/planner.db.
- Один сервер `src/server.mjs` (API + статика + фоновые сторожа),
  фронт — ванильный JS в `public/assets/*.js`, вкладки по ролям.
- Прод: Proxmox LXC (Debian), Docker Compose, приложение слушает :3000,
  nginx (443, TLS Let's Encrypt) проксирует; вход сотрудников изнутри
  сети — https://100.100.10.77, снаружи — https://planner.pegasavto.ru.
- Тайл-сервер карты — СВОЙ: /opt/tiles (russia.mbtiles из OSM-экстракта,
  tileserver-gl на 127.0.0.1:8081, nginx `/tiles/` с кэшем 30 дн).
- Telegram: оба бота (сотрудники, водители) живут в приложении; входящие
  вебхуки `/api/telegram/webhook/{staff|driver}` через 443; исходящие —
  api.telegram.org, при блокировке — релей (app_meta telegram_api_host,
  воркер в tools/telegram-relay-worker.js).
- Мониторинг: Pilot-GPS (blade.pilot-gps.com), позиции/датчики/одометры;
  лимит 8 rps, X-Node обязателен.

## Концепция живых нормативов (обязательная рамка)

Полный текст — CLAUDE.md. Кратко: никаких зашитых бизнес-констант;
пороги — из настроек (цели руководителя) или выучены из фактов с
клампом здравого смысла и формулой-фолбэком; каждый контур отчитывается
о дрейфе; автоматика рекомендует — человек решает; эффект меряется
данными Пилота. Реестр процессов — кнопка «🧠 Живые нормативы» у
руководителя (/api/self-tuning): коэффициент дорог, медианы плеч
(км и часы), ворота П/В, честный одометр, сегменты A/B/C/D, переменная
себестоимость, порог подгона, доверие автоназначению, норма ремзоны,
зазор стыковки кругов, две ночные санации.

## Ключевые защиты данных (сентябрь 2026)

- Километры: 3 слоя (коридор чтения прямая×1,05…×1,65; эксклюзивные
  дни + кламп на записи; санация раздутых планов).
- Транзит: 3 слоя (коридор дорог км/80…км/22+8; кламп ворот 0,5–8 ч;
  санация — только сжатие, не раньше окна клиента и плановых точек).
- Факт рейса: лестница CAN-одометр → GPS-трек → эксклюзивные календарные
  дни → «не измерено» (вне обучения и инвентаризации).
- Местоположение: истина — конструкция (последняя выгрузка/перегон),
  GPS — сверка; расхождения ловит инвентаризация («Мониторинг молчит»,
  «GPS расходится с конструкцией»).
- Температура рефа: каждый отсчёт датчика несёт свою свежесть; старше
  3 ч — «датчик молчит», без тревог по протухшему.

## Текущие фронты (22.09.2026)

- ПЕРЕСТРОЙКА РЕСУРСА (главный проект): этапы 1–2 готовы (график
  /schedule + мосты), руководитель лично ведёт план и заказывает
  доработки; гейт этапа 3 — покрытие плана фактом ≥85% (сейчас ~71%,
  сторож уведомит; решение «Ждём 85%»); к плану нужно подключить
  ресурсников. Этап 3: явка/табель ↔ факт-слой, доплаты сверхвахты.
  Этап 4: вывод старой таблицы закреплений.
- Проект бригад (справочник, старшие бригадиры с правом писать свою
  бригаду, поле logist зарезервировано) — ждёт «делай» руководителя.
- Единый канон отчётности сведён (эта ревизия); дальше следить, чтобы
  новые отчёты брали цифры только из parkReportData/opsReportData.
- Выручка сентября: прогноз ~146 млн бНДС при плане 165 — разрыв на
  контроле (тонкие субботы: клиенты с закрытой выходной приёмкой,
  задания продажам ушли); самарский кластер простоя — без разбора.
- Telegram: работает через релей руководителя (app_meta
  telegram_api_host → Cloudflare-воркер); tgApi вне
  NODE_ENV=production — no-op (урок инцидента «Погрузок: 0»).
- Зелёный коридор автоназначения: остановлен до доверия 80% (факт ~27%).
- Ремзона: этап 1 запущен; этап 2 ждёт целевой межсервисный интервал ТО.
- Звенья рейса: копятся; этап 2 — перевод отчётов на звенья.
- Хостер: внешний порт 22 проброшен не на наш сервер — тикет за
  руководителем; ssh на прод ТОЛЬКО root@100.100.10.77.

## Документация

- CLAUDE.md — правила разработки и рабочий цикл (главное).
- docs/JOURNAL.md — журнал изменений (каждый коммит).
- public/assets/guide.js (GUIDES) — инструкции сотрудников, обновляются
  тем же коммитом, что меняет поведение.
- Память ассистента — ~/.claude/projects/-Users-aleksey-pegas-planner/memory/.
